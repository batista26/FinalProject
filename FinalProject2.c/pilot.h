#ifndef PILOT_H
#define PILOT_H

// Initializes the pilot light pin
void pilot_Init(void);

// Turns the pilot light ON
void pilot_On(void);

// Turns the pilot light OFF
void pilot_Off(void);

#endif // PILOT_H
