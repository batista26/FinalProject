#include <msp430.h>
#include "SYSTEM.h"

void main(){
// Initialize 


while(1) {
    //Start Ignitor 
    // Open Pilot Valve
    // Wait 2 seconds
    //Check if flame present 
    while(temp< threshold)
    {
        if (flame is not present)
            break;
        if (temp > threshold)
            break;

    }


    _bias_SR_register(CPUOFF);
}

}

